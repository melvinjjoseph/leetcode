class Solution:
    def stoneGame(self, piles: List[int]) -> bool:
        # alice=0
        # bob=0
        # while(len(piles)>0):
        #     if piles[0]>piles[-1]:
        #         alice+=piles[0]
        #         piles.pop(0)
        #     else:
        #         alice+=piles[-1]
        #         piles.pop()
        #     if piles[0]>piles[-1]:
        #         bob+=piles[0]
        #         piles.pop(0)
        #     else:
        #         bob+=piles[-1]
        #         piles.pop()
        # if alice>bob:
        #     return True
        # return False
        return True